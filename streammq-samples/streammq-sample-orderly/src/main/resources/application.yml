spring:
  application:
    name: streammq-sample-orderly

streammq:
  enabled: true
  namespace: orderly
  # 完整配置请参考 README「顺序消息」章节与「配置参考」

# Redisson 配置（示例用单机），生产环境请使用集群配置
redisson:
  singleServerConfig:
    address: "redis://127.0.0.1:6379"
    database: 0
