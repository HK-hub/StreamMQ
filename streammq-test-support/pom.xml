<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.streammq</groupId>
        <artifactId>streammq-parent</artifactId>
        <version>0.1.1</version>
    </parent>

    <artifactId>streammq-test-support</artifactId>
    <name>StreamMQ Test Support Core</name>
    <description>StreamMQ 测试基础设施公共件：Redis 可用性探测等零依赖测试工具。作为 streammq-test 的可传递依赖一同发布到 Maven Central。</description>

    <!--
      零内部依赖的叶子模块：任何模块都可以安全地以 test scope 依赖它，
      而不会与 streammq-test（依赖 core/redisson/starter 的顶层聚合工具包）形成环。

      注意：本模块必须随 streammq-test 一同发布到 Maven Central。streammq-test 的
      StreamMQTestBase 在运行期调用本模块的 RedisAvailability，若本模块被 excludeArtifacts
      排除，使用方将得到一个无法修复的 NoClassDefFoundError（该坐标在中央仓库不存在）。
    -->
    <dependencies>
        <!-- 无 -->
    </dependencies>
</project>
