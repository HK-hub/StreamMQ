<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.streammq</groupId>
        <artifactId>streammq-parent</artifactId>
        <version>0.1.2</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>streammq-kubernetes</artifactId>
    <name>StreamMQ Cloud Kubernetes</name>
    <description>StreamMQ 云原生增强模块：K8s Operator、CRD、配置热更新、自动扩缩容、优雅上下线、HPA 指标暴露。</description>

    <dependencies>
        <!-- Core：监听器容器、工具类（provided：由运行时 starter 提供） -->
        <dependency>
            <groupId>io.github.streammq</groupId>
            <artifactId>streammq-core</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Fabric8 Kubernetes Client：Operator 实现核心依赖 -->
        <dependency>
            <groupId>io.fabric8</groupId>
            <artifactId>kubernetes-client</artifactId>
            <version>6.13.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- Diagnostics 积压探针 SPI（provided：可选集成。存在 BacklogProbe Bean 时 HPA 以真实
             XLEN/XPENDING 刷新 lag 指标；缺失时由用户自定义 HpaMetricsProvider 生产者提供，跳过决策
             一定伴随限频 WARN，不静默失效）。该构件不在 BOM 中，需显式声明版本。 -->
        <dependency>
            <groupId>io.github.streammq</groupId>
            <artifactId>streammq-diagnostics</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Jackson: JSON 处理 CRD 对象 -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot Starter Web：REST 探针端点（provided：运行时由使用者提供） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot Actuator：HealthIndicator 集成（provided：运行时由使用者提供） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- SLF4J（provided：日志门面） -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 测试：fabric8 Mock Server（CRUD 模式），版本与 kubernetes-client 对齐。
             用于 Operator/HPA 的回写与幂等性回归（K7/K8） -->
        <dependency>
            <groupId>io.fabric8</groupId>
            <artifactId>kubernetes-server-mock</artifactId>
            <version>6.13.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
